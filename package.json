{
  "name": "telegram-booking",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "node scripts/deploy.mjs --push",
    "deploy:check": "node scripts/deploy.mjs"
  }
}
